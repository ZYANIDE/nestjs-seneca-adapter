/**
 * Node environment values for NODE_ENV
 * that are expected to occur
 */
export declare enum NodeEnvironment {
    local = "local",
    test = "test",
    dev = "development",
    prod = "production"
}
